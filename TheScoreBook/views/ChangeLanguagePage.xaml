<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:localisation="clr-namespace:TheScoreBook.localisation;assembly=TheScoreBook"
             x:Class="TheScoreBook.Views.ChangeLanguagePage"
             x:Name="ChangeLangPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Label Text="{localisation:Translate SelectLanguage}"></Label>
            <Picker ItemsSource = "{Binding Languages}"
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"></Picker>
            
            <Button Text="{localisation:Translate Done}"
                    Command="{Binding ChangeLanguageCommand, Mode=OneTime}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>