<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:localisation="clr-namespace:TheScoreBook.localisation;assembly=TheScoreBook"
             x:Class="TheScoreBook.views.GeneralContainer"
             Padding="0">
    <ContentPage.Content>
        <StackLayout>
            <Grid x:Name="Layout" VerticalOptions="FillAndExpand"/>
            <StackLayout
                Margin="0"
                Padding="0"
                VerticalOptions="End">
                <Grid 
                    x:Name="BottomBar"
                    VerticalOptions="End"
                    Margin="0"
                    Padding="0"
                    BackgroundColor="Gray">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Text="{localisation:Translate Scores}" Clicked="ScoreButtonOnClicked" />
                    <Button Grid.Column="1" Text="{localisation:Translate Shoot}" Clicked="ShootButtonOnClicked" />
                    <Button Grid.Column="2" Text="{localisation:Translate Profile}" Clicked="ProfileButtonOnClicked" />
                </Grid>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>